#include <iostream>
#include <fstream>
#include <string.h>
#include <fstream>
#include <algorithm>
#include <vector>
using namespace std;

#include "maze.h"

int main(void){
    system("chcp 1251");
    vector <dot> B;
    B.push_back(dot(1,2));
    find(B.begin(),B.end(),dot(1,2));
}
